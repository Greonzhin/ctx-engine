# GEMINI.md - generated by ctx-engine

Source of truth: `.ctx-engine/rules.yaml`

Use ctx-engine as the project context gateway.

MCP endpoint:

```text
http://127.0.0.1:7331/mcp
```

Use sandboxing where available. Use offline mode for sensitive repositories.

Generated Gemini settings should use `mcpServers.ctx-engine.httpUrl` for the ctx-engine Streamable HTTP endpoint.
