from __future__ import annotations


def conflict_notes() -> list[str]:
    return [
        "Current local code and tests outrank local docs, Context7 public docs, and memory.",
        "Unverified memory is advisory only.",
    ]
